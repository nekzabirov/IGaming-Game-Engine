-- `created_at` объявлен NOT NULL без значения по умолчанию, и путь, которым раунд заводится по
-- колбэку вендора, его не заполнял: первая же нога КАЖДОГО раунда падала на ограничении, а
-- оператор отвечал хабу INTERNAL — кодом, по которому часть вендоров начинает цикл отката.
--
-- Код теперь ставит отметку сам; значение по умолчанию — чтобы следующий забывший его путь
-- обошёлся кривым временем вместо потерянного раунда.
ALTER TABLE casino_rounds ALTER COLUMN created_at SET DEFAULT now();
